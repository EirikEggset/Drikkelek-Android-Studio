<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content">

    <EditText
        android:id="@+id/new_player_name"
        style="@style/player_name_style"
        android:layout_width="290dp"
        android:layout_height="66dp"
        android:layout_marginTop="5dp"
        android:paddingRight="65dp"
        android:text=""
        android:textAlignment="textStart"
        android:textSize="20dp"
        android:textStyle="bold"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <androidx.appcompat.widget.AppCompatButton
        android:id="@+id/btn_remove_player"
        android:layout_width="50dp"
        android:layout_height="50dp"
        android:layout_marginEnd="10dp"
        android:background="@null"
        android:gravity="center"
        android:padding="0dp"
        android:text="╳"
        android:textAlignment="center"
        android:textColor="@color/white"
        android:textSize="20sp"
        app:layout_constraintBottom_toBottomOf="@+id/new_player_name"
        app:layout_constraintEnd_toEndOf="@+id/new_player_name"
        app:layout_constraintTop_toTopOf="@+id/new_player_name" />
</androidx.constraintlayout.widget.ConstraintLayout>